Q.1 Create a button , on click of which new Heading tag h1 should be added with text as "MERN stack" on the screen above button ?
Ans: function helo(){
    document.getElementById("heading").innerText="Mern Stack"
    }
 
Q.2 Write code to get 1st H1 element from a webpage using DOM?
Ans: function touchme() {
    document.getElementsByTagName("h1")[0].innerText="namste"
    document.getElementsByTagName("h1")[1].innerText="javascript"
    document.getElementsByTagName("h1")[2].innerText="welcome"
    document.getElementsByTagName("h1")[3].innerText="To"
    document.getElementsByTagName("h1")[4].innerText="india"
    }
   
Q.3 Write code to implement timer clock using JS -- display HH:MM:SS -- the time should keep updating every second ?
Ans: function time(){
    let i= new Date().toLocaleTimeString();
    document.getElementById("clock").innerText=i;
    }
    let sec = 1000;
    setInterval(time, sec);
    document.getElementsByTagName("Div")
   
Q.4 Create an HTML page having content as Hello world and a button named Replace Text. When user will click on this button page content should be changed to "Welcome to Elevation academy"
Ans:  function nam(){
    document.getElementById("heading").innerText="Welcome to Elevation academy"
    }
 
Q.5 Create an HTML page having content as Hello world and a button named "Hide Text." When user will click on this button hide the "Hello World" text ?
Ans : function clickhere(){
    document.getElementById("webbutton").style.display="none"
    }
    document.getElementsByTagName
    
 Q.6 Given an array of 0's and 1's find out number of 0's ?
 Ans:  function numberOfZeros(array) {
       var zeros = [];
       var ones=[]
       for(var i = 0; i < array.length; i ++){
        if(array[i] === 0) {
            zeros.push(array[i]);
        }
       }
       console.log("Numbers of zeros ="+zeros.length);
       for(var j = 0; j < array.length; j ++){
       if(array[j] === 1) {
            ones.push(array[j]);
        }
       }
       console.log("Numbers of ones ="+ones.length);
       }
       numberOfZeros([4, 0, 1, 5, 0]);
       
Q.6 Given an array find out total no. of odd and even nos.
Ans:  const array = [1, 2, 3, 4, 5];
      let odd = 0;
      let even = 0;
      for (let i = 0; i < array.length; i++) {
        if (array[i] & 1 == 1)
            odd++;
        else
            even++;
      }
      console.log("Even =", even);
      console.log("Odd = ", odd);      

Q.7 Given a string find out number of vowels ?
Ans: const countVowels = str =>
     Array.from(str) .filter(letter => 'aeiou'.includes(letter)).length;
     console.log(countVowels('abcdefghijklmnopqrstuvwxyz'));
     
Q.8 Write a code to create two objects with 2 properties each, one will be string and other will be an array. Create a function to check if all the elements of the arrays in both the objects are same
Ans: obj={name:"Jai",age:23}
    arr=["Jai",23]
    console.log((arr))
    console.log((obj))
    function myname(){
    const obj1Length = Object.keys(obj).length;
    const obj2Length = Object.keys(arr).length;
    if (obj1Length === obj2Length) {
    return Object.keys(obj1Length).every(
        key => obj2.hasOwnProperty(key)
            && obj2[key] === obj1[key]);
    }
    return false;
   }
   console.log(myname())
